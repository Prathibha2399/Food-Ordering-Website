import React from "react";
import ReactDOM from "react-dom/client";


const header = <h1 id="heading">Hello React Learners, How are you feeling???....</h1>
console.log(header);
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(header);
